package pl.tablehub.mobile.util

import pl.tablehub.mobile.BuildConfig

object Constants {
    const val BACKEND_REST_IP = BuildConfig.BACKEND_REST_IP
    const val BACKEND_WS_IP = BuildConfig.BACKEND_WS_IP
}